- **有什么用？**
	- `math/rand` 包是 Go 语言中用于生成伪随机数的工具。
	- **伪随机数：**并非真正的随机数，而是通过一个初始种子（Seed）和确定性算法（固定的数学公式）计算出来的一串数字序列。序列是可预测的，只要种子相同，序列就相同。
	- **种子：**种子（Seed）是算法开始计算时需要的起始值。
- `func Float64() float64`
	- **作用：**它是一个随机数生成函数，用于返回一个类型为 `float64` 的伪随机浮点数。这个随机数位于半开区间 `[0.0, 1.0)` 内。
-