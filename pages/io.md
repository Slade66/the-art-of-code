- [[type Reader interface]]
- [[func Copy]]
- `func ReadAll(r Reader) ([]byte, error)`
	- **作用：**
		- ReadAll 会从 `r`（读取源）中持续读取数据，直到出现错误或到达文件结尾（EOF），并返回读取到的所有数据。
	- **注意：**
		- **不要用于未知大小的输入：**`ReadAll` 会一直读到 EOF，如果数据流很大（例如网络响应、上传文件、日志流），它会把所有内容都读进内存，没有限制的话，很容易导致内存暴涨甚至 OOM（内存溢出）。
-